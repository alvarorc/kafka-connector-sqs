package com.tarmac.kafka;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class SQSClient {
    private static final Logger log = LoggerFactory.getLogger(SQSSinkTask.class);

    SQSSinkConnectorConfig config;

    public SQSClient(SQSSinkConnectorConfig config) {
        this.config = config;
    }

    public
}
